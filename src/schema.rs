table! {
    stocks (id) {
        id -> Int4,
        shortname -> Varchar,
        longname -> Varchar,
        volumepricetrend -> Int4,
        momentumoscillator -> Int4,
        simplemovingaverage -> Int4,
        exponentialmovingaverage -> Int4,
        trendline -> Int4,
    }
}
